query MonsterQuery($limit: Int!, $skip: Int) {
  monsters(limit: $limit, skip: $skip) {
    index
    name
    imagePath: image

    strength
    dexterity
    constitution
    intelligence
    wisdom
    charisma

    description: desc
    alignment
    size
    type
  }
}
